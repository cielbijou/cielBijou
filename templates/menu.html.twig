<div class="d-flex align-items-center">
    <img class="me-3 rounded-circle shadow-sm" width="55" height="55" src="{{ asset('images/divers/Ciel.png') }}" alt="Logo Ciel Bijou">
    <nav class="navbar navbar-expand-lg navbar-light bg-light flex-grow-1 border-bottom shadow-sm">
        <div class="container px-3 px-lg-4">
            <a class="navbar-brand fw-bold text-primary" href="{{ path('accueil') }}">Ciel Bijou</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarSupportedContent">
                <ul class="navbar-nav me-auto mb-2 mb-lg-0 ms-lg-4">
                    <li class="nav-item">
                        <a class="nav-link active" aria-current="page" href="{{ path('accueil') }}">Accueil</a>
                    </li>
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                            Magasin
                        </a>
                        <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                            <li><a class="dropdown-item" href="{{ path('catalogue') }}">Tous les produits</a></li>
                            <li><a class="dropdown-item" href="{{ path('catalogue_CielBijou') }}">Produits Ciel Bijou</a></li>
                            <li><a class="dropdown-item" href="{{ path('catalogue_Promotion') }}">Promotion en cours</a></li>
                        </ul>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('contact') }}">Contact</a>
                    </li>
                    {% if not is_granted('IS_AUTHENTICATED_REMEMBERED') %}
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('app_login') }}">Connexion</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('app_register') }}">S'inscrire <i class="bi bi-person-plus"></i></a>
                        </li>
                    {% else %}
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('profil_index') }}">Profil <i class="bi bi-person-circle"></i></a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('app_logout') }}">Déconnexion <i class="bi bi-box-arrow-right"></i></a>
                        </li>
                    {% endif %}
                </ul>
                <form method="get" action="{{ path('product_search') }}" class="d-flex me-3 position-relative">
                    <input id="search-input" class="form-control me-2" type="search" name="q" placeholder="Rechercher..." aria-label="Search">
                    <div id="suggestions" class="list-group position-absolute w-100" style="display: none;"></div>
                    <button class="btn btn-outline-primary" type="submit">Chercher</button>
                </form>
                <a href="{{ path('panier') }}" class="btn btn-outline-dark d-flex align-items-center">
                    <i class="bi-cart-fill me-1"></i>
                    Panier
                    <span id="labelNbProd" class="badge bg-dark text-white ms-1 rounded-pill">{{ app.session.get('nbproduitpanier') }}</span>
                </a>
            </div>
        </div>
    </nav>
</div>

{% block stylesheets %}
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
<link href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap-icons/1.10.0/font/bootstrap-icons.min.css" rel="stylesheet">
<style>
.navbar {
    background-color: #f8f9fa;
    border-bottom: 2px solid #e0e0e0;
}
.navbar-brand {
    font-weight: 700;
    font-size: 1.25rem;
}
.nav-item .nav-link {
    color: #333 !important;
    font-weight: 500;
    transition: color 0.3s ease, background-color 0.3s ease;
}
.nav-item .nav-link:hover, .nav-item .nav-link:focus {
    color: #fff !important;
    background-color: #007bff;
    border-radius: 5px;
}
.dropdown-menu {
    border-radius: 5px;
    border: none;
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
}
.dropdown-item:hover {
    background-color: #f1f1f1;
}
.btn-outline-dark {
    border-radius: 30px;
    transition: all 0.3s ease;
}
.btn-outline-dark:hover {
    background-color: #007bff;
    border-color: #007bff;
    color: white;
}
.badge.bg-dark {
    font-size: 14px;
}
</style>
{% endblock %}
